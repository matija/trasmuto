## PRD v4: Dock Drop, Compact Window, Completion Actions

## Task — Dock-Icon Drop (DONE)

### Infrastructure (from Phase 3, verified complete)
- `fileAssociations` in tauri.conf.json registers app for all 11 video extensions
- `RunEvent::Opened` handler in lib.rs: shows window, filters supported extensions, calls `dispatch_conversion` per file
- `dispatch_conversion` in convert.rs: shared helper used by both window-drop (via `convert_file` command) and dock-drop paths
- `PendingJobStarts` buffer + `take_pending_job_starts` command: cold-launch race condition handled — jobs started before React mounts are drained on `useJobQueue` init
- `file-opened` event switches frontend to queue tab

### Fix applied this task
- Non-video dock drop (e.g. `.txt`): window now shows/focuses before the early return, so the app becomes visible even when no valid files were queued (PRD §1.5)

### Tests
- `cargo clippy -- -D warnings` clean

## Task — Compact, Intricate Window (DONE)

### Window Size
- Updated tauri.conf.json: width 480→400, height 400→320, minWidth 360→340, minHeight 320→260

### Title Bar
- Height reduced to 36 px (h-9); background set to `--bg-raised` to create two-surface contrast
- Segmented control remains in title bar row (already there from v3)

### Segmented Control ("Instrument Panel" style)
- Container: 22 px height, `rounded-full`, background `--bg-base` (inset from `--bg-raised` title bar)
- Active pill: `--accent-bg` fill + `--accent` text (120 ms ease); no shadow/glow
- Labels: 12 px, `font-medium`, `tracking-[0.02em]`

### Empty State
- Removed accent-colored circle wrapper; bare 36×36 icon in `--fg-muted` at 40% opacity
- Headline: 13 px, `font-medium`, `--fg`. "Drop a video to convert."
- Subtext: 11 px, `--fg-muted`. "Or drag onto the dock icon."
- Layout: `gap-1.5`, centered

### Content Area
- JobQueue scroll container: `px-3 pt-2 pb-3` (deliberate slight bottom-heaviness per PRD §2.2)
- JobItem: removed `mx-1`, reduced to `px-2 py-2.5`

### Tests
- All 13 frontend tests pass; `tsc --noEmit` clean

## PRD v3: UI Polish

## Task 6 — Native-Style Settings Panel (DONE)

### 6.1 Section Groups
- Two inset grouped cards: `OUTPUT` and `BEHAVIOR`, each with a `rounded-xl bg-[--bg-raised]` container
- 10px ALL-CAPS `tracking-widest` section label above each group

### 6.2 Row Layout
- Full-width rows with `px-4 py-2.5`; label left (`13px font-medium --fg`), optional description below (`11px --fg-muted`), control right-aligned
- Hairline `--divider` between rows via `.settings-group > .settings-row:not(:last-child)` CSS sibling rule
- Wide variant (CRF slider, ffmpeg path) stacks control below the label

### 6.3 Controls
- **Toggle**: pure-CSS pill (28×17 px, accent thumb, 150 ms transition) replaces the checkbox for "Open file when done"
- **CustomSelect**: button + popover styled as NSMenu (`rounded-lg`, shadow, `13px` rows with `8px` vertical padding, accent color for selected row) replaces all native `<select>` elements
- **Text input** (ffmpeg path): `height: 22px`, `rounded-md`, inset border

### 6.4 Scrolling
- Settings scroll container uses `scrollbar-width: none` + `::-webkit-scrollbar { display: none }` via `.settings-scroll` class
- Added `-webkit-overflow-scrolling: touch`

### Tests
- All 13 frontend tests pass; `tsc --noEmit` clean

## Task 5 — Reduced Window Transparency (DONE)

### 5.1 Window Material
- Already using `WindowEffect::Sidebar` in lib.rs — no Rust change needed

### 5.2 CSS Background Fallback
- Added `--bg-base` (light: `rgba(246,246,248,0.96)`, dark: `rgba(28,28,30,0.96)`)
- Added `--bg-raised` (light: `rgba(255,255,255,0.94)`, dark: `rgba(44,44,46,0.95)`)
- Body background changed from `transparent` to `var(--bg-base)`; window now reads as a solid surface

### 5.3 Title Bar
- Added `border-b border-[color:var(--divider)]` to the title bar div for a subtle separator

### Tests
- All 13 frontend tests pass; `tsc --noEmit` clean

## Task 4 — General UI Polish (DONE)

### 4.1 Typography
- Filename in `JobItem`: bumped to `14px`, kept `font-medium`
- Status badges: removed `uppercase tracking-wide` CSS (labels already sentence-case)
- Empty-state headline: bumped to `text-[14px] font-semibold`

### 4.2 Spacing and Layout
- `JobItem` vertical padding: `py-2.5` → `py-3`
- Title bar height: `h-11` → `h-10`
- Progress bar height: `h-1` → `h-[3px]` (both determinate and indeterminate)

### 4.3 Segmented Control
- Container: `rounded-md` → `rounded-full`
- Active pill: `rounded-[5px]` → `rounded-full`
- Inactive labels: already had `font-medium` on button base class (no change needed)

### 4.4 Job Item — Done State
- Added 12×12 checkmark SVG in `--success` color next to done status badge
- Filename dimmed to `--fg-muted` when job is `done`

### 4.5 Empty State
- Video icon wrapped in 56×56 tinted circle (`--accent-bg` background, `--accent` stroke)
- Icon size bumped from 44×44 to 48×48 inside the circle
- Subtext font reduced to `11px` with `max-w-[160px]` and `leading-snug` for clean two-line wrap

### Tests
- All 13 frontend tests pass; `tsc --noEmit` clean

## Task 3 — Auto-Open on Completion (DONE)

### 3.1 Setting
- Added `open_after_conversion: bool` (default: `false`) to `ConversionSettings` in `models.rs`
- Added `openAfterConversion: boolean` to TypeScript `ConversionSettings` in `types.ts`
- Added `openAfterConversion: false` to `DEFAULT_SETTINGS` in `useConversionSettings.ts`
- Exported `useSettingsStore` from `useConversionSettings.ts` so job queue can read setting reactively

### 3.2 Behavior
- In `useJobQueue.ts`, `onConversionComplete` callback: reads `settings.openAfterConversion` and current job list from stores
- Calls `openPath(outputPath)` via `@tauri-apps/plugin-opener` only when: setting is enabled, `outputPath` is valid, and no other job is still running
- Uses `useJobQueueStore.getState()` / `useSettingsStore.getState()` to avoid stale closures

### 3.3 Manual Reveal
- Renamed "Reveal" button to "Show in Finder" in `JobItem.tsx`

### UI
- Added "Open file when done" checkbox toggle at the bottom of `SettingsPanel.tsx`

### Tests
- Updated all test fixtures to include `openAfterConversion: false`
- Updated `JobItem.test.tsx` assertion to match new "Show in Finder" label
- All 13 frontend tests pass; clippy clean

## Task 2 — Improved Drop Experience (DONE)

### 2.1 Drop Overlay
- Film-strip SVG icon replaces the upload arrow
- Border changed from dashed to solid with pulsing glow animation (`drop-pulse` keyframe, 1.6s ease-in-out infinite)
- Fade-in (100ms ease-out) on enter, fade-out (80ms ease-in) on leave via `overlay-in`/`overlay-out` keyframes
- Count-aware label: "Drop 2 videos to convert" for multi-file drags, "Drop to convert" for single
- Drag state refactored: `dragActive` + `dragExiting` + `dragCount`; exit timer clears correctly on re-enter and unmount

### 2.2 Post-Drop Feedback
- `JobItem` outer div gets `animate-job-enter` class: translateY +6px → 0, opacity 0 → 1, 150ms ease-out on mount
- 16×16 film-strip SVG placeholder icon added to the left of the filename

## Task 1 — Default Window Size (DONE)

- Updated `src-tauri/tauri.conf.json`: width 600→480, height 500→400
- Added minWidth: 360 and minHeight: 320 to allow resizing without clipping content

## PRD: Working DMG Release via CI

## Phase 3 — Smoke test full push-to-download flow (DONE)

- Bumped version to 0.2.0 in src-tauri/tauri.conf.json and src-tauri/Cargo.toml; updated Cargo.lock
- Committed to main; CI tag job will push v0.2.0 after CI passes
- Tag push was failing with 403: PAT_TOKEN secret was not set; fixed by switching to github.token (GITHUB_TOKEN with contents:write permission)
- Committed fix: fix(ci): use github.token for tag push instead of missing PAT_TOKEN (7bd8cff)
- Pushed to main; CI will now retry the tag job and push v0.2.0
- Root cause of release not triggering: GITHUB_TOKEN-pushed tags do not trigger other workflows (GitHub anti-loop policy); v0.2.0 tag existed but release.yml never ran
- Fixed ci.yml: tag job now dispatches `gh workflow run release.yml --ref $TAG -f tag=$TAG` after a new tag is pushed (1d6a7c2)
- release.yml was failing with "Resource not accessible by integration" when creating GitHub releases; two root causes:
  1. Repo default workflow token permissions were "read-only" — fixed via GitHub API (PUT /actions/permissions/workflow, default_workflow_permissions=write)
  2. `gh workflow run release.yml --ref v0.2.0` used the OLD release.yml from the tag; fixed by triggering with `--ref main` instead
- Upgraded tauri-apps/tauri-action from @v0 to @v0.6.2; added workflow-level `permissions: contents: write` (3287f2c)
- Triggered release from main ref: `gh workflow run release.yml --ref main -f tag=v0.2.0` (run 24564455830) — SUCCESS
- Verification: `gh release download v0.2.0 --pattern "*.dmg"` downloaded Trasmuto_0.2.0_aarch64.dmg; `file` reports "zlib compressed data" (valid DMG)
- NOTE: DMG mounts on macOS still needs manual verification (step 4 of Phase 3 done criteria)

## Phase 1 — Diagnose (DONE)

- Checked the two most recent `release.yml` workflow runs via `gh run list`
- Run 24536491152 (tag v0.1.0, workflow_dispatch): build compiled successfully but failed at bundle time
- Failure: `security: SecKeychainItemImport: One or more parameters passed to a function were not valid`
- Root cause: `tauri-action@v0` attempted to import a signing keychain because `APPLE_CERTIFICATE` env var was passed (resolves to empty string when secret is unset), which caused `security import` to fail
- The release was never created (draft or otherwise) because the bundle step errored before tauri-action could create it

## Phase 2 — Fix the release workflow (DONE)

- Removed all Apple code-signing env vars (`APPLE_CERTIFICATE`, `APPLE_CERTIFICATE_PASSWORD`, `APPLE_SIGNING_IDENTITY`, `APPLE_ID`, `APPLE_PASSWORD`, `APPLE_TEAM_ID`) from `release.yml`
- Code signing is a non-goal per the PRD; passing empty secrets was causing tauri-action to attempt and fail keychain import
- Without those env vars, tauri-action will produce an unsigned `.app` and `.dmg` — which is the desired output

---

## Phase 1 — Scaffold (DONE)

- Scaffolded Tauri v2 + React 19 + TypeScript project using create-tauri-app
- Renamed app to "Trasmuto" in Cargo.toml, tauri.conf.json, main.rs
- Configured window: 600×500, overlay title bar, hidden title, macOS-native feel
- Added Tauri plugins: notification, store, dialog (Cargo + npm)
- Added Zustand to npm dependencies
- Created full Rust module structure:
  - src/models.rs — ConversionSettings, ConversionJob, ConversionProgress, FileInfo, all enums
  - src/commands/{convert,probe,settings,mod}.rs — all 6 commands stubbed (return dummy data)
  - src/ffmpeg/{finder,builder,progress,mod}.rs — binary discovery + arg builder + progress parser stubs
- Wired RunEvent::Opened: filters supported video extensions, logs paths, emits file-opened event, shows/focuses window
- Updated capabilities/default.json with new plugin permissions
- Verified: cargo build succeeds with 0 errors

## Phase 2 — ffmpeg Core (DONE)

- finder.rs: binary discovery with Homebrew path fallbacks + `which` PATH fallback; added find_ffprobe()
- probe.rs: real ffprobe invocation, parses JSON output for duration, resolution, codecs
- convert.rs: spawns ffmpeg as async child process, reads -progress pipe:1 stdout line-by-line,
  emits conversion-progress / conversion-complete / conversion-error events via AppHandle;
  ActiveJobs state (Arc<Mutex<HashMap>>) stores live Child handles for cancellation
- cancel_conversion: removes child from map and kills the process
- settings.rs: get_ffmpeg_path now calls finder::find_ffmpeg
- lib.rs: registers ActiveJobs::default() as Tauri managed state
- cargo build succeeds with 0 errors

## Phase 3 — Dock Drop Integration (DONE)

- Extracted `dispatch_conversion(input_path, settings, app)` from `convert_file` in convert.rs
  — both the Tauri command and the dock-drop handler share the same ffmpeg spawn logic
- `convert_file` command now delegates to `dispatch_conversion` (uses app.state() internally)
- `dispatch_conversion` exported via commands/mod.rs
- `RunEvent::Opened` handler in lib.rs now:
  - Parses file:// URLs and filters by supported extension (unchanged)
  - Shows + focuses main window and emits `file-opened` to frontend (unchanged)
  - Loads `ConversionSettings::default()` (Phase 4 will replace with store lookup)
  - Spawns one `tauri::async_runtime::spawn` per file, calling `dispatch_conversion`
- cargo build succeeds with 0 errors

## Phase 4 — Settings Persistence (DONE)

- settings.rs: `get_settings` and `save_settings` now backed by `tauri-plugin-store` (settings.json)
- `load_settings(app: &AppHandle) -> ConversionSettings` helper added — sync, falls back to defaults
- Exported `load_settings` from commands/mod.rs
- `RunEvent::Opened` in lib.rs now calls `load_settings(app)` instead of `ConversionSettings::default()`
- cargo build succeeds with 0 errors

## Phase 5 — Frontend UI (DONE)

- Installed Tailwind CSS v4 via @tailwindcss/vite plugin; dark macOS-like theme (zinc palette + sky accent)
- src/lib/types.ts — TypeScript types mirroring all Rust models (ConversionJob, ConversionSettings, enums, FileInfo)
- src/lib/tauri-commands.ts — typed invoke() + listen() wrappers for all 6 commands and 5 events
- src/hooks/useJobQueue.ts — Zustand store; subscribes to job-started / conversion-progress / conversion-complete / conversion-error;
  probes file on job-started to get durationMs for accurate progress percentage; exposes cancelJob
- src/hooks/useConversionSettings.ts — loads settings from store on mount, saves on every change
- src/components/JobItem.tsx — filename, status badge (Pending/Converting/Done/Failed/Cancelled), progress bar
  (determinate when duration known, animated indeterminate otherwise), speed indicator, Reveal/Cancel buttons
- src/components/JobQueue.tsx — scrollable job list; empty state with icon; "Clear completed" footer
- src/components/SettingsPanel.tsx — all settings as selects/slider; CRF/preset rows hidden for HW/copy codecs;
  auto-saves on every change
- src/App.tsx — overlay title bar with drag region; Queue/Settings tab buttons; switches to Queue on file-opened event
- src/index.css — @import "tailwindcss"; .drag/.no-drag utilities; progress-slide keyframe for indeterminate bar
- lib.rs: emit job-started {jobId, inputPath} after dispatch_conversion so frontend can show file names
- cargo build and tsc --noEmit both pass with 0 errors

## Phase 6 — Notifications (DONE)

- `tauri_plugin_notification::NotificationExt` imported in convert.rs and lib.rs
- On job success: native macOS notification "Conversion complete / <filename> is ready"
- On ffmpeg error exit: "Conversion failed / <filename> could not be converted"
- On unexpected wait error: "Conversion failed / An unexpected error occurred"
- `.setup()` block in lib.rs calls `app.notification().request_permission()` on first launch
- cargo build succeeds with 0 errors

## Phase 7 — Build & Distribution (DONE)

- Added `fileAssociations` to `bundle` in tauri.conf.json:
  - All 11 supported video extensions registered as "Video File" with role "Editor"
  - Enables macOS to associate these file types with Trasmuto after `.app` install
  - This makes dock-drop and "Open With" work for installed builds
- Created `.github/workflows/release.yml` using `tauri-apps/tauri-action@v0`:
  - Triggers on `v*` tags (push) and manual `workflow_dispatch`
  - Runs on `macos-latest`; uses Node 22, stable Rust, rust-cache for fast rebuilds
  - Env vars for optional Apple code signing + notarization (APPLE_CERTIFICATE, APPLE_ID, etc.) — secrets are optional; workflow still produces unsigned `.app`/`.dmg` without them
  - Creates a draft GitHub Release with tag name + artifacts attached
- Icons already existed from scaffold phase; no regeneration needed
- cargo build succeeds with 0 errors

## Phase 8 — Tests (DONE)

- 25 unit tests added across three Rust modules:
  - commands/convert.rs: 5 tests for make_output_path (non-mp4 keeps stem, mp4 gets -converted suffix, output in same dir, dots in stem, .mov input)
  - ffmpeg/builder.rs: 12 tests covering H.264/H.265/HW/Copy codecs, CRF/preset presence, resolution scale filter, AAC/copy/strip audio, faststart flag, arg ordering
  - ffmpeg/progress.rs: 8 tests for parse_progress_line, parse_out_time_ms, and parse_speed (valid values, N/A, x-suffix stripping)
- 2 integration tests in src-tauri/tests/conversion.rs:
  - non_mp4_input_creates_output_in_same_folder: generates a real .mkv with ffmpeg lavfi, converts it, asserts sample.mp4 appears in the same temp dir
  - mp4_input_creates_converted_suffix_in_same_folder: same but .mp4 input, asserts sample-converted.mp4 is created without overwriting the original
- Added tempfile = "3" as dev-dependency for temp directory management in integration tests
- Made models and ffmpeg modules pub in lib.rs so integration tests can access ConversionSettings and build_args
- Made make_output_path pub(crate) so unit tests within convert.rs can access it
- All 27 tests pass with 0 warnings

## Phase 16 — CI Gate Before Release (DONE)

- Implemented R6 via Option B (deferred/branch protection)
- Added a NOTE comment to `release.yml` explaining that the CI gate is enforced via branch protection on `main`, not an inline job dependency
- Added "Releasing" section to `README.md` with step-by-step release instructions and a one-time branch protection setup guide (require `ci` status check on `main`)
- Updated `PRD.md` Phase 4 to record the decision and close out the requirement
- All PRD requirements (R1–R6) are now satisfied

## Phase 15 — Release Body Tag Name (DONE)

- Updated `releaseBody` in `release.yml` to include the tag name: `Release ${{ github.ref_name }}. See the assets below to download this version.`
- Satisfies R3 acceptance criterion: release body includes the tag name and a note directing users to the assets section

## Phase 14 — Version Consistency Check (DONE)

- Added `Verify version consistency` step to `release.yml` before the build steps
- Step strips leading `v` from the git tag, then extracts versions from `src-tauri/tauri.conf.json` (via `jq`) and `src-tauri/Cargo.toml` (via `grep`/`sed`)
- Fails with a human-readable message (e.g. `Tag v1.2.0 does not match app version 1.1.0`) if either file disagrees with the tag
- Satisfies R2 acceptance criteria

## Phase 13 — CI Binary Build (DONE)

- Added `targets: aarch64-apple-darwin` to the Rust toolchain step in ci.yml
- Added final `Tauri build` step: `npx tauri build --target aarch64-apple-darwin`
- CI now catches packaging regressions on every push to main and every PR (R1)
- Artifacts are not uploaded; build step exits non-zero on failure

## Phase 12 — Release Workflow Hardening (DONE)

- Narrowed tag trigger from `v*` to `v*.*.*` (semver only, per PRD)
- Added `fetch-depth: 0` to checkout so tauri-action can read full git history
- Added `aarch64-apple-darwin` Rust target for Apple Silicon builds
- Added prerelease detection step: tags containing `-beta`, `-alpha`, or `-rc` set `prerelease: true`
- Changed `releaseDraft: false` so releases publish immediately on success
- Added `if: failure()` step that marks any partially-created release as draft on build failure

## Phase 11 — CI Workflow (DONE)

- Created `.github/workflows/ci.yml` triggered on push to `main` and all PRs
- Steps: checkout, Node 22 + Rust stable (with clippy component), Cargo + npm cache, npm ci, typecheck, lint, frontend tests (vitest), cargo clippy -D warnings, cargo test
- Unit tests only — no ffmpeg required; satisfies PRD Section 3.4

## Phase 10 — Code Refactoring & Dead Code Removal (DONE)

- Removed ConversionJob, JobStatus, ConversionProgress from models.rs — these structs were never used in Rust code; the frontend manages its own job state
- Removed println! debug line from lib.rs RunEvent::Opened handler
- Removed dead getFfmpegPath and onFileOpened exports from tauri-commands.ts (never consumed in any component or hook)
- Consolidated duplicate cancelConversion import in useJobQueue.ts into a single import block
- Removed console.error development artifact from App.tsx drag-drop handler
- Added ESLint (eslint + @eslint/js + typescript-eslint) with eslint.config.js; added npm run lint script to package.json
- All acceptance criteria pass: cargo clippy -- -D warnings (0 warnings), cargo test (27 tests pass), tsc --noEmit (0 errors), npm run lint (0 errors), npm run test (13 tests pass)

## Phase 9 — Frontend Tests (DONE)

- Installed vitest 4, @testing-library/react, @testing-library/jest-dom, jsdom as devDependencies
- Created vitest.config.ts: jsdom environment, globals: true, test-setup.ts setupFile
- Added "test" and "typecheck" scripts to package.json
- Added "vitest/globals" and "@testing-library/jest-dom" types to tsconfig.json
- 13 frontend unit tests across 5 files:
  - src/lib/tauri-commands.test.ts: 4 tests verifying convertFile, cancelConversion, getSettings, saveSettings each call invoke() with the correct command name and arguments
  - src/hooks/useConversionSettings.test.ts: 2 tests — getSettings called on mount, saveSettings called with updated value on update()
  - src/hooks/useJobQueue.test.ts: 4 tests — conversion-progress updates outTimeMs/speed/status, conversion-complete sets done+outputPath, conversion-error sets failed+error, cancelJob calls cancelConversion and marks job cancelled
  - src/components/JobItem.test.tsx: 2 smoke tests — running job shows filename/progress bar/speed/cancel button, done job shows Done badge and Reveal button
  - src/components/SettingsPanel.test.tsx: 1 smoke test — all 6 setting controls visible with default settings (h264, aac)
- All 13 tests pass with npm run test
